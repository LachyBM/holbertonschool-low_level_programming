#include <stdio.h>
#include "main.h"

/**
 * rev_string- reverses string
 * @s:  char
 * Return: 0
 */

void rev_string(char *s)
{
	int length = 0;

	while (s[length] != '\0')
	{
		length++;
	}

	while (length >= 0)
	{
		char letter = s[length];
		char 

		printf("%c",letter);
		length--;
		i++;
	}
	printf("\n");
}
